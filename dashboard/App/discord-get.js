const colors = require('colors');

module.exports = async (client) => {
    console.log(`
    > Récupération des données de ${client.user.tag}.
    > Transmission des données au serveur web.
    `.bold.green)

    async function getInformations() {

    }
    //getInformations();
}